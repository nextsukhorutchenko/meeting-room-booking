# Specs

These human-reviewed Playwright plans describe only deterministic flows backed
by `e2e/seed.spec.ts`, `prisma/seed-test.ts`, and checked-in E2E fixtures.
Planner, generator, and healer output remains a proposal until a developer
reviews the resulting diff and reruns the deterministic suite.
